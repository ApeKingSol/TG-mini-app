# Copy this file to .env and fill in real values. Only vars prefixed with
# VITE_ are exposed to client-side code (see https://vite.dev/guide/env-and-mode).

# Supabase project (Settings -> API in the Supabase dashboard)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
